# 데일리 리포트 — 2026-07-14

## 요약
오전에 `open_go_kr` 대체 소스 4종(korea_kr/me/moel/moel_policy) PR #19 병합.
이후 RD-2 C/S 파일럿을 실제로 돌리다 PRISM 실데이터(department)를 LLM이
지어내던 버그를 발견·수정했고, 발주처(문민주) 지적으로 "C/S 합성 PDF도
O트랙 수준의 레이아웃 다양성이 필요"해져 Genalog를 검토했으나 설치 실패로
폐기, 대신 reportlab/Pillow/opencv 조합으로 기관유형별 레이아웃 + 보안마크
노이즈를 자체 구현해 파일럿 PDF 64건을 생성했다(아직 커밋 안 됨). 별도로
`feat/#17-pdf-text-extraction` 브랜치(원격에만 존재, 이 워킹트리에는 없음)에서
C/S 트랙 span 증강 파이프라인(반복헤더 주석 → 조항별 후보탐지 → LLM 치환
파일럿)이 3개 커밋으로 계속 진행됨.

## 오늘 완료된 작업 (커밋/PR 기준)

1. **open_go_kr 대체 소스 4종 추가 및 PR #19 병합** (`c200853`, 10:24 병합) —
   어제 밤 조사한 korea_kr 외에 3개 어댑터가 더 추가된 채로 병합됨:
   - `korea_kr`: 정책브리핑 보도자료(전 부처 통합, 약 45만 건)
   - `me`: 기후에너지환경부 행정규칙(고시·훈령·예규), 법제처 국가법령정보센터
     미러 — 메타데이터 전용
   - `moel`: 고용노동부 행정규칙 — me와 doc_type 공유, 본문 전문+첨부파일 포함
   - `moel_policy`: 고용노동부 정책자료실 — 제목 키워드로 4종 세분류
   - 전부 robots.txt 실사 확인 후 착수, `rd2_test` DB 실제 수집 검증
     (Conformance PASS, quarantine 0건).

## 별도 브랜치 진행 — `feat/#17-pdf-text-extraction` (원격, 이 워킹트리 아님)

이 브랜치는 로컬 워킹트리에는 체크아웃돼 있지 않고 `origin`에만 존재 — 다른
세션/머신에서 작업 후 푸시된 것으로 보임. 오늘 커밋 3개가 새로 쌓임:

1. **반복 헤더/쪽번호 span 주석(`is_boilerplate`) 추가** (`1b7ceb5`) —
   문서 전체 페이지의 30% 이상(최소 3페이지)에서 같은 좌표·같은 문구로
   반복되는 span에 표시만 얹은 사본을 생성(`annotate.py`). 원본 span_id는
   보존해 나중에 PDF 레이아웃 재합성 시 손실 없음.
2. **정보공개법 제9조 5·6·7·8호 조항별 기밀도 상승 후보 span 탐지**
   (`1929ebe`) — annotate된 2,164개 문서 전체에서 조항별 정규식/키워드로
   후보를 찾아 `data/candidates/clause_{5,6,7,8}.jsonl`에 저장
   (`candidates.py`). 실측 중 8호(개발/지정/고시 등) 키워드가 부동산과
   무관한 내용(커리큘럼 개발 등)을 대량 오탐하던 문제, 5·7호도 유사 문제를
   발견해 조항 맥락이 뚜렷한 복합어 위주로 좁힘.
3. **LLM 기반 hard-example span 증강 파일럿 구현** (`d2a8163`) —
   annotate+후보탐지 결과를 gpt-4o-mini로 보내 조항별 기밀도 상승 문구로
   치환(`augmentation/llm_augment.py`, `run_llm_augment.py`, strict JSON
   Schema 응답). 20건 파일럿(4개 조항×5문서) 중 발견한 문제 수정:
   - 초기 프롬프트가 민감도를 오히려 낮추는(순화) 방향으로 동작 →
     "공개정보→비공개/내부정보" 재구성을 명시하는 few-shot 예시 추가
   - 원문 대비 길이 비율(0.5~1.5배)·원문 잔존 여부 자동 검증해 탈락시키는
     로직 추가
   - 6호(개인정보) 후보 탐지가 문장 중간의 "담당자" 부분매칭까지 잡던 문제를
     "라벨:값" 패턴 또는 20자 이하 단독 문구로 좁힘
   - 6호 개인정보 형식을 후보 길이에 따라 유연화(짧으면 이름만, 길면
     이름+번호)
   - 컨텍스트 인계용 `AUGMENTATION_STATUS.md` 추가

## 진행 중 (아직 커밋 안 됨, 현재 브랜치)

- **PRISM C/S 시드의 실제 department를 LLM이 지어내던 버그 수정** —
  `generate_cs_pilot.py`가 SQL에서 `department` 컬럼을 안 뽑고
  `generate_seeded_body()`가 매번 LLM에게 부서명을 지어내게 하고
  있었음(`ordering_agency`는 이미 실데이터를 그대로 쓰는데 department만
  예외). PRISM C/S 시드 대상 행(EC2 769건/로컬 910건) 전부 실제 department
  값을 갖고 있어 실데이터를 두고 안 쓰고 있었던 것으로 확인. `SeedInput`에
  `department` 필드 추가, 프롬프트가 department 제공 시 그대로 쓰라고
  지시, 최종 저장은 LLM 응답과 무관하게 실데이터 우선. `src/rd2/generators/
  generate.py`, `scripts/generate_cs_pilot.py`, 테스트 3건 추가 — 전체
  240개 테스트 통과 확인.
- **버그 발견(미수정, P2로 기록): `generate_clause_document()`가 S 조항에서도
  항상 `DisclosureStatus.CLOSED` 반환** — plan-eng-review Codex Outside
  Voice에서 발견. 이번 파일럿에선 실제로 이 폴백을 타는 게 C-1(PRISM 실보유
  1건)뿐이라 당장 오염은 없지만, S 문서가 이 경로를 타게 되면 라벨 불일치
  발생. `DisclosureStatus.PARTIAL`을 S에 매핑하는 게 맞는지 확인 필요.
- **Genalog 폐기, Pillow/opencv로 자체 구현** — 발주처(문민주) 지적:
  RD-1이 레이아웃을 기밀성 판별 신호로 쓸 수 있어 C/S 합성 문서도 O트랙
  수준의 레이아웃 다양성이 필요해짐. `pip install genalog`를 이 프로젝트
  Python 3.14.6에서 시도 → 실제 설치 실패(2020년대 numpy 고정 버전, 소스
  빌드 깨짐, 재현 확인) — 유지보수 중단 상태로 채택 불가 확정. 대신:
  - `agency_categories.py`: 기관유형 5종(중앙부처/광역자치단체/연구기관/
    교육청/공공기관)별 레이아웃 정의
  - `doc_type_inference.py`: 지금까지 C/S 문서가 전부 doc_type
    `SYNTHETIC_DOCUMENT` 고정이라 레이아웃 다양성이 없던 문제 — 조항번호를
    `naming.py`의 기존 `DOC_TYPE_*`로 매핑(조항5는 감사/입찰계약/인사 등
    키워드로 세분화, 새 분류체계는 안 만듦)
  - `pdf_render.py`: reportlab으로 기관유형별 텍스트 선택 가능한 디지털 PDF
    레이아웃 생성
  - `security_mark.py`: 보안마크 이미지 하나를 Pillow로 그린 뒤 opencv로
    흐림/회전/잉크번짐 노이즈 적용해 삽입
  - `scripts/generate_cs_pilot.py --render-pdf`로 기존 CSV 재사용(LLM 재호출
    없이) PDF 생성 — 파일럿 결과물: `cs_pilot_output.csv`(1,309건),
    `cs_pilot_pdfs/`(PDF 64건 + 보안마크/워터마크 이미지 8건)
  - `pyproject.toml`에 `reportlab>=4.0`, `Pillow>=10.0`,
    `opencv-python-headless>=4.9` 의존성 추가
  - 신규 테스트 5개 파일(`test_agency_categories.py`,
    `test_doc_type_inference.py`, `test_pdf_render.py`,
    `test_security_mark.py`, `test_generate_cs_pilot.py`)
  - **미완료로 남긴 부분**: 원래 TODOS가 요구했던 "실제 스캔 문서처럼 보이는"
    전체 페이지 래스터라이즈(전체 흐림/회전/왜곡)는 의도적으로 범위에서
    뺌(발주처 요구는 텍스트 선택 가능한 디지털 PDF 유지 + 이미지 요소만
    노이즈였음) — 나중에 "진짜 스캔본처럼 보이는 PDF"가 필요해지면 재검토.

## DB 수집 현황

### 로컬 (`rd2_dump`, 이 워킹트리 `.env` 기준)

| source | 어제 오후 | 오늘 | 증감 |
|---|---|---|---|
| PRISM | 6,304 | 7,485 | +1,181 |
| alio | 5,221 | 5,221 | 0 |
| synthetic-llm | 1 | 1 | 0 |
| **합계** | 11,526 | **12,707** | **+1,181** |

quarantine 누적: 653 → 658 (+5). 오늘 늘어난 건 전부 PRISM — 신규 어댑터
4종은 로컬에는 아직 소량 검증만 반영됨.

### EC2 (`rd2_dump`, 운영 인스턴스 — 사용자 직접 조회)

| source | 건수 |
|---|---|
| alio | 5,221 |
| PRISM | 7,485 |
| korea_kr | 3,948 |
| moel_policy | 4,000 |
| moel | 3,499 |
| me | 1,500 |
| synthetic-llm | 1 |
| **합계** | **25,654** |

**오늘 가장 큰 수확**: 오전에 병합한 신규 어댑터 4종(korea_kr/me/moel/
moel_policy)이 어제 리포트엔 "`rd2_test`에서 5건씩만 검증"으로 기록돼
있었는데, 오늘 EC2에서 실제로 대량 수집이 돌아 **12,947건**(korea_kr
3,948 + moel_policy 4,000 + moel 3,499 + me 1,500)이 신규 확보됨. alio/
PRISM은 EC2와 로컬 건수가 동일해 두 환경이 같은 시점 기준 동기화된
것으로 보임.

**확인 필요(다음 세션)**: EC2가 정확히 어느 커밋으로 돌았는지(로컬
`c200853` 기준인지), EC2 쪽 quarantine 건수, 신규 4종의 doc_type별 분포 —
아직 조회 못함.

## 테스트 현황
- 전체 테스트 스위트: **240개 전부 통과** 확인(방금 재실행).
- `deploy/README.md`, `deploy/rd2-crawler.service`는 어제(07-13) 이후
  추가 변경 없음 — 여전히 미커밋 상태로 대기 중.

## 내일 / 후속 할 일
- 오늘 만든 department 버그 수정, Genalog 대체 구현(agency_categories/
  doc_type_inference/pdf_render/security_mark) 커밋 및 PR 정리
- `generate_clause_document()` S조항 CLOSED 폴백 버그 수정 여부 결정
  (PARTIAL 매핑 관례 확인 먼저)
- `feat/#17-pdf-text-extraction` 브랜치(원격) 상태 동기화 — 로컬 워킹트리에
  없어 이번 세션에서 직접 확인 못함, 다음에 fetch/checkout해서 검토 필요
- EC2 신규 소스 4종(12,947건) quarantine/doc_type 분포 확인, 로컬 DB와
  동기화 여부 정리
- cs_pilot 파일럿(1,309건 CSV, PDF 64건) 품질 리뷰 후 본 규모 생성 여부 결정
- `AUGMENTATION_STATUS.md`(원격 브랜치) 인계 내용 확인
- open_go_kr 자동 수집 재개 금지 — 여전히 유효(TODOS.md P0)
