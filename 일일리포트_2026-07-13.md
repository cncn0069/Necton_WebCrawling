# 데일리 리포트 — 2026-07-13

## 요약
알리오/교육부(moe) 어댑터 안정화, 원문정보(정보공개포털) 첨부파일 다운로드
체인(wonmun) 리버스엔지니어링 완료 + 실제 DB 저장까지 검증, PDF 텍스트 span
추출 파이프라인 신규 추가. PRISM은 체크포인트 기준 6,680건까지 처리(실제 저장
6,304건). open_go_kr은 `--doc-type-cap` 옵션 추가 후 체크포인트를 0으로 리셋,
오늘 밤 전체 재수집 시작.

## PRISM 수집 현황
- 체크포인트 처리 위치(`rd2.db.prism_checkpoint.json`): **6,680건**
- 실제 DB 저장: **6,304건** — 공개(O) 4,890 / 비공개(S) 1,311 / 부분공개(C) 103
  (체크포인트 위치와의 차이 376건은 중복 스킵/quarantine)
- EC2 배포용 systemd 서비스(`rd2-crawler.service`)가 `collect_prism.py 8000`을
  실행하도록 준비됨 — conda 환경 경로로 오늘 수정 완료(아래 참고)

## 오늘 완료된 작업 (커밋/PR 기준)

1. **알리오(ALIO) 반복 제목 버그 수정** (`8c2fb0d`, PR #12 병합) —
   `browse_client.py`에 페이지네이션 시 같은 제목이 반복 수집되던 버그 수정.
2. **merge 충돌 부호(`===`) 잔여 제거** (`46ecce3`, PR #14 병합)
3. **교육부(moe.go.kr) 재정·예산 정보 게시판 어댑터 추가** (`51c5e60`,
   `fcc31b9`, `1fa78dd`, PR #15 병합) — 신규 어댑터, 목록 파싱 `<tr>` 중첩
   중복수집 버그 수정, `department` 필드를 `always_filled`에서 제외.
3. **원문정보(정보공개포털) 첨부파일 다운로드 어댑터 추가** (`8ce56b2`, PR #18
   병합) — 오늘 가장 큰 작업:
   - wonmun 다운로드 체인(4단계: `wonmunFileRequest` →
     `wonmunFileFilter` → `wonmunChangePdf` → `wonmunFileDownload`)을
     httpx 콜드세션으로는 재현 실패, 최종적으로 브라우저 세션 내부
     `fetch()`를 단계별로 분리 호출하는 방식으로 안정화(실사 5건 중 4건 성공).
   - `OriginalInfoAdapter` + `scripts/collect_orginl_info.py` +
     conformance 계약 추가, 실사 5건 수집으로 검증(2건 저장, 3건 정상
     quarantine, 크래시 없음).
   - 알려진 한계 4건 기록: 부분 다운로드 시 고아 파일, `with_retry`가
     영구 실패도 3회 재시도, browse.exe 탭 격리 미구현, PDF 변환
     타임아웃(30초) 부족 사례 1건 관측.
4. **PDF 텍스트 위치정보(span) 추출 파이프라인 추가** (`eea570d`,
   브랜치 `feat/#17-pdf-text-extraction`에 푸시 완료, **PR 미생성**) —
   C/S 트랙 기밀도 상승 span 합성 준비 단계. PyMuPDF 글자 단위 span을
   줄/문단 단위로 재병합, 표 셀·2단 비교표 좌우 컬럼 오합병 방지, 다운로드
   실패 HTML 스텁 매직바이트 필터링, `data/` 전체 포맷 무결성 스캔
   스크립트 포함.

## 진행 중 (아직 커밋 안 됨)

- **AWS EC2 배포 노트 대폭 업데이트** (`deploy/README.md`,
  `deploy/rd2-crawler.service`): venv → conda 전환(rd2 계정 sudo 불가 이슈
  포함), RDS 준비 전 EC2 로컬 MariaDB 임시 운영 절차(로컬 덤프 →
  rsync/scp 전송 → 복원 → 이후 RDS로 재이관) 신규 문서화, systemd가
  `conda activate` 훅을 안 태우는 문제 대응(ExecStart 절대경로 고정).
- **`collect_open_go_kr.py`에 `--doc-type-cap` 옵션 추가** — 공개(OPEN)+
  본문 있는 문서에만 doc_type당 상한 적용(비공개/부분공개는 상한 예외,
  C/S 시드 가치 우선). `storage/db.py`에 `count_by_doc_type()` 헬퍼 추가.
- **open_go_kr 체크포인트를 0으로 리셋** — 오늘 밤 전체 재수집을 위한 준비.
- `dump.sql.gz`(로컬 MariaDB 덤프, EC2 이관용) 로컬에 생성됨.

## DB 현황 스냅샷 (`rd2_dump`, 방금 조회)

| source | 건수 |
|---|---|
| alio | 5,221 |
| PRISM | 6,304 |
| synthetic-llm | 1 |
| **합계** | **11,526** |

quarantine 누적: 653건

## 오후 세션 추가 작업

### ⚠️ 중요 — open_go_kr 전체 재수집 계획 취소(위 "내일 할 일"과 반대 결정)

오전 리포트의 "open_go_kr 전체 재수집(nohup 백그라운드) 진행 상황 확인" 항목은
**실행 안 됨, 그리고 앞으로도 보류**로 뒤집혔다. EC2 배포 중 open_go_kr이
EC2 클라우드 IP를 소프트 차단("접속지연" 안내로 위장)하는 걸 발견해 원인을
추적하다, **`open.go.kr`의 `robots.txt`가 사이트 전체 크롤링을 명시적으로
금지**(`Disallow: /`, 루트 `/` 한 페이지만 예외)하고 있음을 확인함. 로컬이든
EC2든 무관하게 위반이라 **open_go_kr 자동 수집을 즉시 중단**하고 TODOS.md에
P0 항목으로 기록(대체 소스 조사 필요, 프로젝트 발주자 법적 확인 필요).

PRISM은 같은 이유로 한때 의심했으나(robots.txt 요청이 WAF 차단 응답을 반환)
정상 브라우저 User-Agent로 재확인한 결과 `Disallow:`(빈 값=전체 허용)로 무혐의
확인 — curl 기본 UA가 WAF에 걸렸던 것뿐이었음. PRISM 수집은 재개 가능.

### EC2 배포 진행 (하루 종일 소요)

- rd2 서비스 계정 생성, 코드를 `/opt/rd2`로 배치, MariaDB 덤프 이관(11,526건
  확인), `data/`(51GB) rsync 완료.
- `rd2-crawler.service` ExecStart 경로 불일치(옛 커밋 `.venv` vs 로컬 미커밋
  conda 경로) 수정, 배포 문서(`deploy/README.md`) 대폭 갱신.
- **gstack browse 데몬 버그 근본 원인 규명**: PRISM 같은 무거운 React SPA에서
  health-check(`page.evaluate`)가 `"Target crashed"`로 실패 → 매번 새 데몬을
  스폰하며 탐색 상태가 리셋됨. bun 설치, gstack 소스 clone+빌드, Playwright
  Chromium 별도 설치, `GSTACK_CHROMIUM_NO_SANDBOX`/`GSTACK_CHROMIUM_NO_GPU`
  우회, swap 2GB 추가까지 시도했으나 이 EC2 인스턴스에서는 PRISM 크래시를
  완전히 해결하지 못함(→ TODOS.md에 Playwright 네이티브 마이그레이션 TODO로
  기록, 오늘은 보류).
- **plan-eng-review 진행**(2회) — swap 추가, `--no-sandbox` 임시 유지 결정,
  Playwright 재작성 시점(오늘 X, 별도 PR)까지 여러 아키텍처 결정을 문서화.

### 신규 어댑터: korea_kr(대한민국 정책브리핑 보도자료)

open_go_kr 중단에 따른 대체 소스로 조사 후 신규 구현:
- `robots.txt` 전체 허용(`Allow: /`) 확인 후 착수. 목록 페이지에 본문 전문이
  이미 포함돼 있어 상세페이지는 첨부파일 조회용으로만 사용하는 구조로 설계.
- `src/rd2/adapters/korea_kr.py`, `scripts/collect_korea_kr.py`,
  `tests/test_korea_kr_adapter.py`(7개 전부 통과) 신규 작성.
  `naming.py`/`conformance.py`/`DOC_TYPES.md`에 `SOURCE_KOREA_KR`/
  `DOC_TYPE_PRESS_RELEASE`(신규 문서유형) 등록.
- `rd2_test` DB(운영 `rd2_dump` 아님)에 실제 5건 수집·저장 검증 완료 —
  질병관리청/농림축산식품부/행정안전부/기후에너지환경부/기획예산처 등
  서로 다른 5개 기관, PDF+HWP/HWPX 파일 11개 정상 다운로드, Conformance PASS.
- 전체 이력 기준 대상 규모 약 **45만 건**(페이지네이션 기준 산정) — open_go_kr
  대비 압도적으로 큰 수집 대상 발견.
- 전체 테스트 스위트(140개) 회귀 없음 확인.

## 내일 / 후속 할 일 (갱신)

- `feat/#17-pdf-text-extraction` PR 오픈 (오전 항목, 아직 미완료)
- **open_go_kr 자동 수집 재개 절대 금지** — 프로젝트 발주자의 법적/정책 확인이
  선행돼야 함(TODOS.md P0)
- korea_kr 전체/대량 수집 진행 여부 결정(대상 약 45만 건 — 목표 규모 논의 필요)
- gstack browse → Python Playwright 네이티브 마이그레이션(별도 PR, EC2에서
  PRISM 상시 운영 시 필요)
- OriginalInfoAdapter 알려진 한계 4건 후속 조치(원자적 파일 저장, 영구실패
  재시도 정책 분리, 탭 격리, PDF 변환 타임아웃 상향)
- EC2 PRISM 서비스는 disable 상태로 둠(오늘 크래시 이슈 미해결) — 로컬에서
  계속 수집 권장
