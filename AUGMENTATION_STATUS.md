**[2026-07-20] 폐기됨** — LLM 치환·PDF 재구성(bbox 기반) 트랙 전체를 범위에서
제외하기로 결정. 좌표(bbox)가 더 이상 필요 없어지면서, 파이프라인 책임은
"텍스트 추출 → annotate → 정규식 기반 후보 탐지"까지로 축소됨. 아래는 폐기 전
설계 이력의 참고용 기록이다(코드는 삭제됨: `llm_augment.py`, `run_llm_augment.py`,
`test_reconstruct_pdf.py`).

# S(민감) 트랙 span 치환 증강 — 진행 현황

이 문서는 새 대화 세션에서 컨텍스트를 이어받기 위한 참고 문서다. 상세 설계
근거·실측 데이터는 각 코드 파일의 docstring/주석에 있으니, 여기서는 전체
그림과 "지금 어디까지 됐는지"만 정리한다.

## 목표

RD-2(C/S/O 문서 분류기 학습데이터 구축 파이프라인)의 일부. 실제 수집한
O(공개) PDF의 일부 텍스트(span)를 "기밀도가 상승한 문구"로 자연스럽게
치환해서, **원본 레이아웃(bbox)을 유지한 채** S(민감) 트랙 학습데이터를
만든다. 최종 산출물의 단위는 **"문서 전체"** — 원본 문서를 통째로
재구성하되 일부 span만 synthetic 텍스트로 바뀐 것 = 학습데이터 1건.
`Document` 스키마와 동일하게 `cso_classification=S`, `cso_sub_clause`=
정보공개법 제9조 조항번호(5~8호)로 라벨링한다.

## 증강 아키텍처 (범위 확정됨)

원래 사용자가 제시한 4개 카테고리(Lexical/Structural/Style/Hard-example)
중 2개만 진행하기로 함:

- **Hard-example 증강** (진행 중, 아래 "완료된 것" 참고): 보안 키워드는
  있지만 실제 보안수준은 다른(또는 그 반대) 문서를 만들어 모델이 키워드만
  보고 판단하는 shortcut learning을 방지. span 단위 치환이라 레이아웃 보존과
  잘 맞음
- **Lexical 증강** (미착수): 의미·보안수준·엔티티는 유지한 채 표현만
  바꾸는 패러프레이즈. O 원본과 Hard-example로 만든 S 문서 둘 다에 적용
  예정. span 단위 치환 방식이 될 것으로 예상(Hard-example 코드와 구조
  유사할 것)
- **Structural(구조)·Style(문체) 증강**: **보류/제외 결정됨** — 문장
  재배열·문체 전환은 bbox 레이아웃과 근본적으로 충돌하고, 실제 문서가 이미
  부처(molit/mohw/moe)별로 문체가 달라 자연스러운 다양성이 어느 정도
  커버한다고 판단. 나중에 분류기가 구조/문체에 과적합되면 재검토

## 완료된 것 (전부 커밋됨, 브랜치 `feat/#17-pdf-text-extraction`)

1. **PDF 텍스트 추출** — `src/rd2/extraction/pdf_text.py`,
   `scripts/extract_pdf_text.py`. molit+mohw+moe 2,164개 PDF 전체 추출
   완료(PRISM 제외, HWP는 별도 설계 필요해 범위 밖). 위치(bbox)·폰트·길이
   보존, 글자 단위로 쪼개지던 걸 줄/문단 단위로 병합, 2단 비교표 안전장치 등
2. **문서 정제/annotate** — `src/rd2/augmentation/annotate.py`,
   `scripts/annotate_documents.py`. 2,164개 전체에 `is_boilerplate`/
   `cleaned_text` 주석 완료(원본 span 삭제·변경 없음). 표 헤더·쪽번호처럼
   반복되는 span을 위치 기반으로 탐지해 표시(짧은 문서 1회성 콘텐츠 오탐
   버그 수정됨)
3. **조항별 후보 탐지** — `src/rd2/augmentation/candidates.py`,
   `scripts/find_candidates.py`. 정보공개법 제9조 5·6·7·8호마다 정규식/
   키워드로 후보 span을 찾아 `data/candidates/clause_{5,6,7,8}.jsonl`에 저장.
   6호(개인정보)만 메커니즘이 다름 — 실제 개인정보를 찾는 게 아니라 자리표시
   성격 span을 찾아 통째로 가상 정보로 교체할 후보를 찾음. 오탐 방지를 위해
   키워드를 여러 번 좁힘(8호의 "개발"/"지정" 등 과광범위 키워드 제거, 6호의
   "라벨:값" 패턴 요구 등) — 최신 개수: 5호 15,172개(1,018문서) / 6호
   3,920개(584문서, 방금 좁힘) / 7호 13,428개(924문서) / 8호 12,433개(838문서)
4. **LLM 증강(Hard-example 핵심 로직)** — `src/rd2/augmentation/llm_augment.py`,
   `scripts/run_llm_augment.py`. 문서 1개+조항 1개당 LLM 호출 1번, 후보 중
   일부를 골라 synthetic 텍스트 생성. `generators/generate.py`와 동일한
   OpenAI 클라이언트 패턴, `response_format`은 strict JSON Schema(필드
   누락 방지). 자동 검증: 길이 비율 0.5~1.5배 벗어나거나 원문이 그대로
   남아있으면(예: 실명+가짜번호만 덧붙임) 자동 탈락. **"기밀도가 실제로
   올라갔는지"는 로직으로 검증 불가 — 표본 육안 확인만 가능**하다는 게
   사용자와 합의된 전제

### 파일럿 테스트 결과 (2026-07-16, 4개 조항 × 5문서, 6호는 후보 탐지 개선 후 재검증까지 완료)
- 5호: 대체로 양호, 가끔 어색한 조어("총괄립장관") — LLM 노이즈로 간주
- 6호: 안전장치(실명 미보존) 확인됨. **후보 탐지 정밀도 문제 발견 후 수정
  완료** — "라벨: 값" 패턴 또는 20자 이하 단독 문구만 인정하도록 좁혀서
  6,906개(721문서) → 3,920개(584문서)로 정리. 재검증 결과 대부분 자연스러운
  이름 치환으로 개선됐으나, `"1.기획담당관 특근매식비"`(13자, 예산 항목명)처럼
  20자 이하 예산 항목이 가끔 여전히 걸리는 잔여 케이스 있음(이전보다 영향은
  훨씬 작음 — 문장 전체가 아니라 짧은 항목명 정도만 대체됨)
- 7호: 대체로 양호, 가끔 주제 이탈("공적연금"→"보건분야" 할루시네이션 1건
  발견) — LLM 노이즈로 간주, 코드로 못 막음
- 8호: 5건 다 양호

## 다음에 할 일

1. **Hard-example 규모 확장**: 지금은 조항당 5문서만 테스트함. 목표는 조항당
   최종 문서 변형 약 2,000개(=LLM 처리 원천 문서 약 400개 × Lexical 증강으로
   5배) — 정확한 배수/최종 목표치는 Lexical 증강 설계가 끝나야 확정
2. **Hard-example 최종 문서 재구성** (미착수): `data/augmented/llm/...`의
   치환 결과를 원본 `data/extracted/...`의 전체 span 리스트에 반영해 통째로
   재구성된 S 문서를 만들고 `Document` 스키마 메타데이터 포함해서 저장
3. **Lexical 증강 설계·구현** (미착수): O 원본 + Hard-example S 문서 둘 다에
   적용, span 단위 치환(길이 유사하게), Hard-example과 구조적으로 유사할 것
4. **HWP 파이프라인** (미착수, 범위 밖으로 유보): 4,146개, PDF와 구조가
   완전히 달라(bbox 대신 문단/런 오프셋) 별도 설계 필요

## "행정처리" 비공개 사유 — 설계 결정 (2026-07-15, 착수 전 단계)

정보공개법 제9조 1~8호(법적 비공개 사유)와 별개로, 실무에서는 **아직 확정되지
않아 절차상 공개되지 않은 상태**(결재 미완료, 공개시점 미도래, 내부 협의중 등)로
문서가 민감/비공개 취급되는 경우가 있다는 문제제기가 있었다. 이건 5~8호 키워드를
넓히는 문제가 아니라 **조문 밖의 완전히 별도 카테고리**다 — 처음에 5호 세부
키워드(수의계약/인사위원회 등)로 잡힌 span에 `"clause": "행정처리"` 태그를 붙이는
식으로 잘못 구현했다가(단순히 5호 확장이었을 뿐), 사용자가 "실제 문서를 이런
요소를 넣어서 민감 문서로 만드는 것"이 목표라고 정정해 방향을 다시 잡았다.
(→ 후속조치: `llm_augment.py`의 `_selection_clause_tag`/`candidates.py`의
`_CLAUSE_5_ADMIN_DETAIL_KEYWORDS` export는 이 잘못된 전제로 만들어진 것이라
되돌림 완료 — 저장된 데모 파일 2건은 이 로직이 실행되기 전이라 오염 없었음.)

### 후보 유형 12개 → 실제로 다룰 수 있는 것 추리기

사용자가 제시한 12개 행정상태 유형(결재미완료/공개시점미도래/문서초안/내부검토중/
첨부미등록/공개심사중/타기관협의/비식별처리중/시스템등록오류/폐기대상/민원처리중/
감사조사진행중) 중:

- **제외 — 텍스트 영역이 아님**: "시스템 등록 오류"(공개여부 미설정 등 순수
  시스템 상태, 문서 텍스트로 표현할 대상 자체가 없음), "첨부파일 미등록"(본문이
  아니라 `other_file_paths` 메타데이터 문제).
- **제외 — 이미 5호와 동일**: "감사·조사 진행중"은 정보공개법 5호("감사·감독·
  검사...내부검토과정")가 이미 포함하는 사유라 별도 카테고리가 아님.
- **부분 제외 — 이미지 합성 필요**: "결재 미완료" 중 "결재자 인장(도장)" 부분은
  이미지 합성 문제라 범위 밖. 다만 "결재 진행중" 같은 **문구 자체**는 텍스트로
  표현 가능해 남긴다.
- **남는 것(텍스트로 표현 가능, 총 8개)**: 공개시점 미도래·문서 초안·내부
  검토중(부서협의)·공개심사 진행중·타기관 협의·비식별 처리중·폐기 대상·민원
  처리중.

### 핵심 기술 제약과 결정

PDF에서 추출한 건 텍스트 span과 그 bbox뿐이다 — 빈 공간 위치도, 이미지(도장·
로고) 위치도 모른다. 그래서 "새 문구를 임의 위치에 삽입"은 불가능하고, 레이아웃을
안전하게 건드릴 수 있는 유일한 방법은 지금 5~8호가 쓰는 것과 동일한 **"이미 존재하는
span을 비슷한 길이로 치환"** 뿐이다. 문제는 5~8호와 달리 "행정처리" 문구는 원문에
자연스럽게 대응되는 소재가 잘 없다는 것.

검토한 3가지 방향:
- **A. 정형 필드(donor span)만 노린다** — 생산일자/작성일 라인, 기안·검토·결재자
  라인, 문서분류·보안등급 라인처럼 공문서에 거의 항상 존재하는 필드를 6호와 같은
  "라벨: 값" 탐지 방식으로 찾아 치환(예: "생산일자: 2024-03-15" → "공개예정일:
  2027-03-01"). 기존 검증된 메커니즘(bbox 보존, 길이비 0.5~1.5배 검증)을 그대로
  재사용 가능.
- B. 반복 헤더/푸터(`is_boilerplate` span)에 문구 추가 — 길이비 제약 위반·부자연스러운
  줄바꿈 위험 커서 보류.
- C. PDF는 안 건드리고 `Document.non_disclosure_reason`(기존 free-text 필드)에만
  기록 — 가장 쉽지만 5~8호와 다른 별도 트랙이 되고, 최종 분류기가 이 필드를
  실제로 학습에 쓰는지 미확인이라 보류.

**결정: A로 진행.** 이유: 이미 검증된 span 치환 파이프라인(bbox·길이비 검증,
6호의 "라벨: 값" 탐지 패턴)을 그대로 재사용할 수 있어 신규 리스크가 가장 작음.
단, 다룰 수 있는 유형이 위 8개 중 "공개시점 미도래"·"결재 진행중"·"공개심사
진행중"처럼 정형 필드에 대응되는 것들로 좁혀진다 — "문서 초안"·"내부 협의중"·
"타기관 협의"·"비식별 처리중"·"폐기 대상"·"민원 처리중"처럼 본문 문장 자체에
자연스럽게 녹아들어야 하는 유형은 donor span을 못 찾으면 이번 방식으로는
어려울 수 있음(다음 설계 단계에서 유형별로 재검토 필요).

**다음 단계(미착수)**: 정형 필드 후보 탐지 로직 설계(`candidates.py`에 6호
"라벨: 값" 패턴과 유사한 방식으로 추가할지, 별도 모듈로 뺄지 결정 필요) — 아직
착수 전.

## 주요 설계 결정 요약 (질문 나올 만한 것들)

- **왜 대표문서 200개 샘플링(클러스터링) 안 함**: 실측 결과 전체 코퍼스
  처리해도 LLM 비용이 $1~3 수준이라 샘플링으로 비용 아낄 이유가 없었고,
  조항별로 필요한 소재가 코퍼스 전역에 흩어져 있어 샘플링하면 특정 조항
  소재를 놓칠 위험이 있었음
- **왜 노이즈 인젝션(숫자 재무작위화) 아이디어가 사라짐**: 원래는 규칙
  기반 5배 증강이었는데, 이후 사용자가 제시한 진짜 요구사항(Lexical 증강)이
  이 역할을 대체함 — LLM이 문단 단위로 패러프레이즈하는 방식
- **transformation 필드가 비어있던 문제**: 자유 텍스트 JSON 프롬프트로는
  LLM이 필드를 종종 빼먹어서, strict JSON Schema(`response_format`)로
  전환해 해결
- **6호 개인정보 형식**: 주민등록번호가 필수 아님(개인정보보호법상 "성명+
  소속+연락처" 조합도 개인정보) — 후보 길이에 따라 이름만/이름+번호 등
  유연하게 선택하도록 프롬프트 설계함
