# 민감데이터 생성 데일리 리포트 — 2026-08-03

- **배치 산출물**: `output/ec2_full_20260803/`
  (`batch_records.jsonl` 242행, `render_payloads.jsonl` 204행)
- **원문 코퍼스**: ALIO 전량 (`alio-*`) 242건, 단일 소스
- **모델**: 판별·생성 `gpt-4o` / 독립 검증 `gpt-4o-mini`
- **생성 모드**: 전 건 `counterfactual` (원문 판별은 242건 모두 O — 비민감 원문에 S를 주입하는 반사실 생성)
- **타깃 지정**: 전 건 `classifier-primary` (수동 `requested_target` 0건)

---

## 1. 처리 결과 요약

| 구분 | 건수 | 비율 |
|---|---:|---:|
| 투입 원문 | 242 | 100% |
| 기술적 생성 성공 | 221 | 91.3% |
| **최종 승인 (`accepted_s`)** | **204** | **84.3%** |
| 재시도 후 제외 (`excluded_after_retry`) | 17 | 7.0% |
| 파이프라인 실패 (`pipeline_failed`) | 21 | 8.7% |

승인 204건이 그대로 `render_payloads.jsonl`로 넘어갔다 — 렌더 게이트 통과분과 승인분이 일치한다.

**재시도는 이번 배치에서 한 건도 건지지 못했다.** 2회 시도한 17건 전부 1차·2차 모두 `assessed_o`로 나와 제외됐다(회수율 0/17). 재시도 비용은 전액 낭비다.

---

## 2. 생성된 민감 유형 분포 (승인 204건)

### 호(號)별

| 호 | 건수 | 비율 |
|---|---:|---:|
| 제5호 (감사·감독·검사·규제) | 190 | 93.1% |
| 제6호 (개인정보) | 6 | 2.9% |
| 제7호 (경영·영업상 비밀) | 5 | 2.5% |
| 제8호 (부동산 투기·매점매석) | 3 | 1.5% |

### 목(目)별

| 목 | 라벨 | 호 | 건수 | 비율 |
|---|---|---|---:|---:|
| `audit_inspection` | 감사·검사 | 5 | 180 | 88.2% |
| `personnel_management` | 인사관리 | 5 | 6 | 2.9% |
| `subject_pii` | 조사대상자 개인정보 | 6 | 5 | 2.5% |
| `security_diagnosis` | 보안진단·취약점 | 7 | 4 | 2.0% |
| `real_estate_speculation` | 부동산 투기 | 8 | 3 | 1.5% |
| `bid_contract` | 입찰계약 | 5 | 2 | 1.0% |
| `decision_review` | 의사결정·내부검토 | 5 | 1 | 0.5% |
| `technology_development` | 기술개발 | 5 | 1 | 0.5% |
| `business_strategy` | 경영전략 | 7 | 1 | 0.5% |
| `personnel_pii` | 인사·채용 개인정보 | 6 | 1 | 0.5% |

택소노미 전체 21개 목 중 **오늘 표본이 생긴 목은 10개**다. 제1~4호는 0건, 제6호는 4개 목 중 2개, 제7호는 5개 목 중 2개, 제8호는 2개 목 중 1개만 채워졌다.

> **커버리지 경고**: 승인분의 88%가 `audit_inspection` 한 목에 몰렸다. 원인은 모델이 아니라 **원문 코퍼스 편중**이다 — 투입 242건 중 179건(74%)이 `audit_material` 문서형이고, 이 문서형은 100% 승인되며 전부 `audit_inspection`으로 흘러간다. 학습·평가용 데이터셋으로 쓰려면 오늘 산출물만으로는 제5호 감사 축 외의 호/목 표본이 사실상 없다.

### 문서형별 (원문 기준)

| 원문 문서형 | 투입 | 승인 | 승인율 |
|---|---:|---:|---:|
| `audit_material` | 179 | 179 | 100% |
| `meeting_minutes` | 27 | 10 | 37% |
| `inspection_report` | 23 | 8 | 35% |
| `report` | 4 | 4 | 100% |
| `investigation_report` | 3 | 3 | 100% |
| (미상) | 6 | 0 | — |

`inspection_report` 투입 23건은 승인 8건 + `source_incompatible` 실패 15건이다(실패 메시지에 잠긴 문서형이 명시돼 있다). 미상 6건은 분류 단계에서 `evidence_invalid`로 끊겨 문서형이 배정되기 전에 실패했다.

`validation_form`은 204건 전부 `source_document_form`과 일치했다 — 문서형 락(form lock)은 정상 동작 중이다.

### 생성 경로 / 근거 수준

| 경로 | 건수 | 원문 근거 수준 |
|---|---:|---|
| `anchored` | 197 | `contextual_anchor_only` |
| `span_seeded` | 24 | `direct_sensitive_span` |

근거 인용(`evidence_quotes`)은 승인분 기준 1건 140, 2건 54, 3건 9, 4건 1. `evidence_from_insertion`은 삽입 유래 표시가 사실상 전 건 `True`로, 반사실 생성 특성상 예상된 값이다.

---

## 3. 실패 분석

### 3-1. 파이프라인 실패 21건

| 코드 | 단계 | 건수 | 내용 |
|---|---|---:|---|
| `source_incompatible` | planning | 15 | 문서형 `inspection_report`를 유지한 채 `audit_inspection` 타깃을 실을 수 있는 목이 없음 |
| `evidence_invalid` | classification | 6 | 분류기가 제시한 인용문이 원문 어디에도 없음 (지어낸 인용 / block 경계 초과) |

- `source_incompatible` 대상: `alio-9631, 9677, 9756, 9797, 9802, 9803, 9809, 9822, 9834, 9878, 9894, 9895, 9907, 9910, 9916`
- `evidence_invalid` 대상: `alio-9667, 9676, 9705, 9782, 9808, 9862`

`source_incompatible` 15건은 전부 동일한 조합(`inspection_report` × `audit_inspection`)이다. 재시도 불가(`retryable: false`)로 표시돼 있으니 모델 문제가 아니라 **택소노미 호환표의 구멍**이다. `inspection_report`에서 살릴 수 있는 목을 열어주거나, 플래너가 이 문서형에 대해 다른 타깃을 1순위로 고르게 해야 한다.

### 3-2. 재시도 후 제외 17건 — 단일 원인

**제외 17건이 전부 `decision_review` × `meeting_minutes` 조합이다.** 다른 조합에서는 제외가 한 건도 없다.

| 타깃 목 | 승인/시도 | 승인율 |
|---|---:|---:|
| `audit_inspection` | 180/180 | 100% |
| `personnel_management` | 6/6 | 100% |
| `subject_pii` | 5/5 | 100% |
| `security_diagnosis` | 4/4 | 100% |
| `real_estate_speculation` | 3/3 | 100% |
| `bid_contract` | 2/2 | 100% |
| 기타 3개 목 | 각 1/1 | 100% |
| **`decision_review`** | **1/18** | **6%** |

`decision_review`를 뺀 나머지 목은 203/203 = 100%다. 즉 오늘 배치의 승인 손실은 **거의 전부 `decision_review` 하나에서 발생**했다. 회의록에 "의사결정 과정·내부검토 자료"를 심는 생성이 검증기(`gpt-4o-mini`)에게 반복적으로 O로 읽힌다 — 생성 프롬프트가 이 목에서 충분히 구체적인 민감 사실을 만들지 못하거나, 검증기가 회의록 형식에서 이 목을 인정하지 않는 쪽이다. 둘 중 어느 쪽인지는 제외 17건의 생성 본문을 검증기 rationale과 나란히 놓고 봐야 갈린다.

---

## 4. 생성 품질

승인 204건 기준.

- **본문 길이**: 최소 161 / 중앙값 468 / 최대 887자
- **블록 수**: 최소 2 / 중앙값 6 / 최대 15
- **블록 종류 분포**: `paragraph` 458, `key_value` 236, `table` 225, `bullet_list` 223, `attachment_reference` 55
- **원문 블록 수**: 최소 12 / 중앙값 69.5 / 최대 1012

### 금지 문형 위반 — 37.7%

생성기 프롬프트가 금지한 "내용의 존재만 말하는" 문형이 **204건 중 77건(37.7%)**에서 나왔다.

| 금지 문형 | 적발 |
|---|---:|
| `본 문서는` | 71 |
| `본 보고서는` | 6 |

문서형별 위반율:

| 문서형 | 위반/승인 | 위반율 |
|---|---:|---:|
| `audit_material` | 72/179 | 40% |
| `inspection_report` | 2/8 | 25% |
| `report` | 2/4 | 50% |
| `investigation_report` | 1/3 | 33% |
| `meeting_minutes` | 0/10 | 0% |

회의록은 0%인데 감사자료는 40%다. 회의록 템플릿은 발화·안건 구조가 강제돼 서술문이 들어갈 자리가 없는 반면, 감사자료 템플릿은 자유 단락을 허용해 모델이 "본 문서는 ~에 관한 …"으로 시작하는 요약문을 붙인다. 문서형별 템플릿 제약 차이가 그대로 위반율로 나타난 것이므로, 프롬프트 문구보다 **감사자료 템플릿의 첫 단락 제약**을 손보는 쪽이 효과가 클 것으로 보인다.

---

## 5. 조치 제안 (효과 순)

1. **`decision_review` × `meeting_minutes` 진단** — 승인 손실 17건 전량. 제외분 생성 본문 + 검증기 rationale 대조로 생성/검증 중 어느 쪽 문제인지 먼저 가른다. 고치기 전까지 이 조합의 재시도는 100% 낭비이므로 재시도를 끄는 것만으로도 즉시 비용이 준다.
2. **`inspection_report` × `audit_inspection` 호환표 구멍 메우기** — 파이프라인 실패 15건 전량. 플래너 단계에서 결정론적으로 막히는 구간이라 모델 호출 전에 해결 가능하다.
3. **감사자료 템플릿 첫 단락 제약 추가** — 금지 문형 위반 77건 중 72건이 여기서 나온다.
4. **코퍼스 다변화** — ALIO 단일 소스 + `audit_material` 74% 편중 탓에 제6~8호 표본이 합쳐서 14건뿐이다. 다음 배치는 원문 문서형 균형을 맞춰 뽑아야 호/목 커버리지가 생긴다.
5. **`evidence_invalid` 6건 모니터링** — 분류기가 인용문을 지어내는 사례. 현재 비율(2.5%)은 낮지만 프롬프트 변경 시 회귀 지표로 추적할 값이다.

---

## 부록: 재현

```bash
python scripts/render_source_generation_flow_report.py output/ec2_full_20260803/batch_records.jsonl
```

문서별 판별 → 계획 → 생성 → 검증 흐름은 위 플로우 리포트에서 확인한다. 본 리포트의 집계는 `batch_records.jsonl` 242행과 `render_payloads.jsonl` 204행에서 직접 산출했다.
