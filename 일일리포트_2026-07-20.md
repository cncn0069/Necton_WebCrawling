# 데일리 리포트 — 2026-07-20 (템플릿 작업 중심)

## 템플릿 12개 → 53개로 확장

- 기존 12개(직접 설계)에 더해, 7개 문서유형별 실물 원본 구조를 대조해 보완한
  41개 템플릿을 추가했다. 조항(1~8호)×문서유형 조합별 전용 템플릿 매트릭스가
  `T1-1~T1-4`부터 `T8-1~T8-7`까지 채워졌다.
- 매트릭스에 없는 조합은 여전히 범용 PDF로 렌더링되며, 각 행은
  `validate_row(spec, row)`로 금지 문구 여부를 렌더링 전에 검증한다.

## 41개 보완 템플릿의 원본 참조 (문서유형별 대표 원본 1건씩 대조)

| 문서유형 | 적용 템플릿 | 원본에서 확인한 구조 |
|---|---|---|
| 공문 | T2-2, T3-2, T3-6, T4-2, T5-7, T6-4, T7-3, T8-5 | 기관명·수신/경유/제목·관련근거 번호문단·가나다 하위문단·붙임·결재선·시행정보 |
| 정책자료 | T1-2, T3-3, T7-7, T8-2 | `참고` 라벨·제목 박스·작성부서·□/ㅇ/* 개요체·하단 공개구분 |
| 보고서 | T1-3, T2-3, T3-5, T4-3, T4-7, T5-8, T5-10, T6-3, T7-1, T7-6, T8-3, T8-6 | 관련근거·보고문·가나다 결과 계층·후속조치·붙임·결재선 |
| 회의록 | T1-4, T2-4, T3-4, T5-6, T6-6, T7-5, T8-4 | 독립 회의록 셸·회의개요·안건번호/안건명/논의내용/논의결과 2열 표·질문 `ㅇ`/답변 `☞` |
| 계획안 | T2-5, T3-7, T4-6, T8-1 | 관련근거·추진개요 2열 표·세부 추진내용·행정사항·붙임·결재선 |
| 승인·품의 | T4-4, T5-9, T6-5, T7-4, T8-7 | 관련근거·승인 요청문·검토내용·처리계획·붙임·미완료 결재선 |
| 회신·통보 | T4-5 | 대외 수신·인사말·통보문·조치내용 표·발신명의·결재선·시행정보 |

전체 대응표와 개별 근거 문서 링크는 `TEMPLATE_SOURCE_MAP.md`에 정리했다.

## 구조 변경: 본문(body)과 서식(form) 분리

- `DocTemplateSpec`에 `form_format` 필드를 추가해, 본문 partial뿐 아니라
  문서의 외곽 셸(상단/하단)도 문서유형별로 분리했다. 정책자료·회의록은 실제
  원본이 표준 시행공문과 다른 독립 서식을 쓰기 때문이다.
- `_DOC_BODY_FORMAT`에 문서유형→본문 매핑 7종을 새로 추가:
  `source_official`, `source_policy_brief`, `source_report`,
  `source_meeting_record`, `source_plan`, `source_approval`,
  `source_notification`.
- `_DOC_FORM_FORMAT`으로 정책자료→`policy_brief_form`, 회의록→
  `meeting_record_form`을 지정하고, 나머지는 기본 `official_form`을 쓴다.
- 템플릿 파일 구조도 이에 맞춰 재편:
  - `templates/bodies/`: `source_*` 7종(승인·회의록·통지·공문·계획·정책브리핑·보고) 신규 추가, 기존 body 13종과 공존
  - `templates/forms/`: `official_form.html`, `meeting_record_form.html`, `policy_brief_form.html` 3종 신규 디렉터리

## 검증/생성 도구 보강

- `generate_template_samples.py`에 원본 대조 검증 로직을 보강했다(125줄 추가).
- 템플릿별 1건만 빠르게 시각 검수하는 명령을 추가:
  `--verify-source-files --samples-per-template 1 --output-dir output/pdf/template_samples_source_review`
- `TEMPLATE_CHECKLIST.md`를 53개 조합 기준으로 갱신하고, "보완 대상" 절을
  "추가 원본 확보 시 보완 대상"으로 좁혀 남은 미확정 항목(T1-1~T4-1, T5-4 등)만
  남겼다.

## 아직 커밋 전 상태

- `TEMPLATE_CHECKLIST.md`, `TEMPLATE_SOURCE_MAP.md`, `doc_templates.py`,
  `generate_template_samples.py`, `pdf_render.py`, `templates/base.html`,
  `templates/document.css`, `templates/official_form.html`은 unstaged.
- `templates/bodies/source_*.html` 7종과 `templates/forms/` 디렉터리는
  untracked 상태로, 다음 커밋에 함께 포함해야 한다.
