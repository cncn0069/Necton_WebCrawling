v1.1


근무일	

| 근무일 | 안순현 | 노혁 |
|---|---|---|
| 1 (07-07 화) | PRISM 착수 + 국가법령정보센터 + 공공데이터포털 |  |
| 2 (07-08 수) | PRISM 마무리 국가기록원 | 알리오/알리오플러스 + 한국조세재정연구원 착수 |
| 3 (07-09 목) | 나라장터  | 한국조세재정연구원 마무리 + 한국보건사회연구원 착수 |
| 4 (07-13 월) | 한국개발연구원(KDI) | 한국보건사회연구원 마무리 |
| 5 (07-14 화) | 한국행정연구원 + 크로스트랙 통합QA | 크로스트랙 통합QA |

## 사이트별 구축 순서 (의존성 그래프)

```
                     ┌─────────────────────────┐
                     │ 공유 인프라 (구축 완료)    │
                     │ SourceAdapter 인터페이스  │
                     │ storage/db.py            │
                     │ schema/models.py          │
                     │ adapters/conformance.py   │
                     │ adapters/browse_client.py │
                     └────────────┬─────────────┘
                                  │ (모든 사이트가 공유, 재구축 불필요)
          ┌───────────┬───────────┼───────────┬────────────┐
          ▼           ▼           ▼           ▼            ▼
   ┌────────────┐┌──────────┐┌─────────┐┌───────────┐┌──────────┐
   │정보공개포털  ││나라장터    ││국가기록원 ││국가법령정보││공공데이터 │
   │[완료+수정]  ││(공식API)  ││(공식API, ││센터        ││포털      │
   │            ││          ││ 일1000건 ││(공식API)  ││(공식API) │
   │            ││          ││ 한도)    ││           ││          │
   └────────────┘└──────────┘└─────────┘└───────────┘└──────────┘
          │                        │
          │ (browse_client 재사용) │ (일일 한도 → 수집기간 별도)
          ▼                        ▼
   ┌────────────┐            ┌──────────────────┐
   │  PRISM      │            │  알리오/알리오플러스│
   │ (스크래핑,   │            │  (경영공시 API)    │
   │  JS 렌더링   │            └──────────────────┘
   │  필요 추정)  │
   └────────────┘
          │ (browse_client 재사용, 개별 사이트 4개)
          ▼
   ┌──────────────────────────────────────────┐
   │ 국책연구기관 4개 (통합 API 없음, 개별 구축)  │
   │ 한국개발연구원(KDI) / 한국행정연구원 /       │
   │ 한국조세재정연구원 / 한국보건사회연구원        │
   └──────────────────────────────────────────┘
```

**순서 근거**: 공식 API 5개(나라장터/국가기록원/국가법령정보센터/공공데이터포털/알리오)는 봇탐지·JS렌더링 문제가 없어 상대적으로 빠르고 리스크가 낮다 — 먼저 처리해 "빠른 승리"로 실제 속도를 재보정한다. PRISM은 `안순현-수집계획수립-20260706.md`에서 이미 우선순위 2위로 확정됐고(비공개 근거 조항이 명시적으로 표시됨 — 오늘 정보공개포털에서 근사 매핑으로 때운 부분을 정확히 채울 수 있는 유일한 소스), `browse_client`의 헤드리스 브라우저 렌더링 기법을 그대로 재사용할 수 있다. 국책연구기관 4개는 통합 API가 없어 사이트마다 개별 조사·구축이 필요하므로 가장 나중, 그리고 사이트 수만큼 반복 비용이 든다.

## PRISM 진행 현황 (2026-07-07 기준)

### 스키마

| 구분 | 필드 | 비고 |
|---|---|---|
| Document 공통 | title, ordering_agency, department, disclosure_status, subject_category, body_text, body_file_path, other_file_paths, non_disclosure_reason, cso_classification, cso_sub_clause, performing_agency, start_date, end_date, source, source_url, doc_type | RD-2 16필드 + source/doc_type/other_file_paths |
| PRISM 고유값 | `doc_type="연구보고서"` 고정, `cso_sub_clause`는 숫자만("5" 등, 1~4호=C·5~8호=S가 disclosure_status보다 항상 우선) | |
| 파일 다운로드 | 상세페이지의 실제 "다운로드" 링크를 클릭 검증(alert 차단 감지)해 통과한 파일만 저장 — disclosure_status 라벨이 아니라 사이트의 실제 파일 단위 접근제어를 그대로 따름 | 비공개/부분공개는 "활용결과보고서"(행정문서)만 통과, 실제 연구내용 보고서는 차단됨 |

### 예상 건수

설계 문서엔 PRISM 단독 목표치가 없음 — O(공개) 20,000건 / C 16,000건 / S 15,000건이 8개 출처 전체 합산 목표. PRISM은 O트랙 최상위 4개 소스 중 하나 + C/S 시드 우선순위 2위.

### 실제 수집 건수 (rd2.db 기준, 20건)

| 분류 | 건수 |
|---|---|
| O (공개) | 14건 |
| S (비공개, 조항 5호) | 4건 |
| S (부분공개) | 2건 |

### 카테고리 (subject_category = 연구분야)

일반공공행정 2, 문화체육관광 2, 통일·외교 1, 환경보호 1, 사회복지 1, 미기재 13(목록에 이 필드가 항상 채워지는 건 아님).

### 실제 용량 (파일 다운로드된 20건 기준 실측)

68개 파일, 총 161MB — 문서당 평균 약 3.4개 파일 / 8.1MB. 이 평균으로 PRISM이 O트랙 20,000건 중 몇 %를 맡을지에 따라 총 용량 추정 가능(예: 10%=2,000건이면 약 16GB, 25%=5,000건이면 약 40GB) — 소스별 배분이 확정돼야 정확한 추정 가능.

### 진행 방식

`scripts/collect_prism.py`로 수집 — `--db`/`--count`/`--skip` 옵션 지원, 매 건 처리 직후 진행 위치를 `<db>.prism_checkpoint.json`에 저장해 중간에 실패해도 다음 실행이 그 위치부터 자동으로 이어감(`--reset-checkpoint`로 처음부터 재시작 가능).

## 보건복지부 입찰안내 게시판 진행 현황 (2026-07-08 기준)

O트랙 신규 출처. `https://www.mohw.go.kr/board.es?mid=a10502000000&bid=0025` — 전자정부
표준프레임워크 게시판(`board.es`/`boardDownload.es`)이라 PRISM과 달리 봇탐지·JS렌더링이
없어 `browse_client` 없이 순수 `httpx`로 목록·상세·첨부파일을 전부 받아온다(첫 사례).

### 스키마

게시판 성격상 전부 공개 공고문이라 `disclosure_status`/`cso_classification`은 항상
공개/O로 고정된다. 대신 이 게시판은 입찰공고/사전규격공개/공모/재공고가 섞여 있어
`doc_type`을 고정값으로 두지 않고 제목 키워드 우선순위(`사전규격`→`재공고`→`공모`→
`입찰`/`공고`로 끝남→fallback `공고`)로 추론한다(`adapters/mohw.py`의
`_infer_doc_type`).

### 실제 수집 건수 (rd2.db 기준, 5건 검증 실행)

| doc_type | 건수 |
|---|---|
| 입찰공고 | 1건 |
| 사전규격공개 | 1건 |
| 공모 | 3건 |

### 진행 방식

`scripts/collect_mohw.py`로 수집 — `[count]` 위치 인자(기본 5)와 `--db`/`--skip` 옵션
지원. PRISM과 달리 클릭 검증이 필요 없어 체크포인트 없이 `skip`만으로 이어서 수집한다.